#!/bin/bash
LOG=/var/log/desktop-to-git.log
echo "*** $(date -R) ***" >> $LOG
cd $HOME/dotfiles && bin/saveApps desktop --quite && git add custom/desktop.list && git commit -m 'Auto Commit of Application List for Desktop' && git push
